# Runtime-only dependencies for the ROS gateway/action workers.
# Deliberately excludes torch/transformers: this Harness delegates inference
# and tokenization to llama.cpp, so pulling CUDA wheels into the ROS image adds
# gigabytes without providing runtime functionality.
fastapi>=0.141.1
httpx>=0.28.1
jinja2>=3.1.6
langgraph>=1.2.11
openai>=3.0.0
python-dotenv>=1.2.2
python-multipart>=0.0.32
